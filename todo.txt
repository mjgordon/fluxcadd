NEW :
fix input stuff
test to make sure running as before works
refactor: move stuff from Fluxcadd to FCWindow
redo lisp section
implement alternate p5 backend (partly to test abstraction)

===============RoboCam===============

Robot IP's:
192.168.2.253
Subnet Mask: 255.255.255.0
Default Gateway:
192.168.2.254

Drawing Tools:
1. Felt Pen
2. Brush (watercolor)
3. Airbrush

=== New Todo ===
Router : ScaleTo Control
Router: Center Button
General: Proper ortho clipping planes
General: Better geometry organization
General: Controller flow like in normal java



Robot (Immediate) TODO:
Merge plot and bake functions
Add parentheses to font
Filepath for export location
Close plotted paths?
continuos mode
points-per-inch
3d Previews of toolpath
angle writing tool with direction of movement
make pencil holder
build base for drawing

Thoughts on robot Topology: 
If an ideal 3d mesh is made of a series of closed, continuous edge loops that
meet in a gridlike structure, then these loops should be able to form the tool
paths for a 3dprinted structure. Although they don't define an ideal stuctural
system, they do form an ideal representational topology. 

===============Param0================

General TODO:
fix axes in non-perspective view
only redraw on updates?
Basic Geometry(Points)
Picking
zooming to a center (can use some of the picking code for this)
fix PointFont
Add screenshots


A new direction: 
Geometry entirely as code, generated from plain text files. 
Mouse is used to view the scene but not to edit it at all.
(I may merely be using this as a way to get out of learning how to unproject 
properly). :P
Editable internally or externally at the same time. 
Grasshopper already has the flowchart method done well, should stay away from
that in any case. 

min (Minimal script) - Designed to get the basic loading and parsing system 
off the ground and to hammer out working from multiple editors
point(10,10,10);
b = point(20,20,20); 
	both of these will put a point into the geometrical memory, but only the 
	second one will leave the point open to future editing in the program.
	
llin (Lisp-like minimal script)
(point 10 10 10)
(set a (point 10 10 10))
(set b (point (>x a 10) (>y a 10) (=z a)))

Something java-like would be easiest for the end user to pick up
Something lisp like would be easiest for me to compile.
